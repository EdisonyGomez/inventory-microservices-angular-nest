generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model InvProduct {
  id        String   @id @default(uuid())
  name      String
  price     Decimal
  stock     Int
  imageUrl  String?
  createdAt DateTime @default(now())

  @@map("inv_products")
}
